apiVersion: cert-platform.io/v1alpha1
kind: GradingCheck
metadata:
  questionId: cka-q012
spec:
  targetCluster: cka-kind
  namespace: backup-inventory
  maxScore: 10
  checks:
    - id: backup-crd-list
      type: remote-file-line-set
      points: 10
      node: cka0002
      file:
        path: /home/ubuntu/an.txt
        lines:
          - backupplans.backup.platform.example.com
          - backups.backup.platform.example.com
          - restores.backup.platform.example.com
        exact: true
